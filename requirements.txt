# Core trading bot dependencies
python-dotenv==1.1.1
requests==2.32.4

# Data analysis (for take_profit_optimizer.py)
pandas==2.3.0

# Dependencies automatically installed by the above packages:
# - certifi (requests dependency)
# - charset-normalizer (requests dependency)  
# - idna (requests dependency)
# - urllib3 (requests dependency)
# - numpy (pandas dependency)
# - python-dateutil (pandas dependency)
# - pytz (pandas dependency)
# - tzdata (pandas dependency)
# - six (pandas dependency)
# - packaging (pandas dependency)